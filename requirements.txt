# Core
aiohttp>=3.9
jinja2>=3.1

# Google APIs
google-api-python-client>=2.100
google-auth>=2.20
google-auth-oauthlib>=1.0
google-auth-httplib2>=0.2

# Notion (direct REST — no SDK needed, just aiohttp)

# Dev convenience
python-dotenv>=1.0
